export function toUpperFirstCase(input: string): string {
  return input.charAt(0).toUpperCase() + input.slice(1);
}

export function toLowerFirstCase(input: string): string {
  return input.charAt(0).toLowerCase() + input.slice(1);
}