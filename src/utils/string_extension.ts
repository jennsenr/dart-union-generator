export function toUpperFirstCase(input: String) {
  return input.charAt(0).toUpperCase() + input.slice(1);
}

export function toLowerFirstCase(input: String) {
  return input.charAt(0).toLowerCase() + input.slice(1);
}